const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function get() { const { data, error } = await supabase.from('students').select('id, student_id, father_name, class_id, classes(class_name, section), profiles(id, full_name, profile_photo_url, mobile)').limit(1); console.log(data); console.error(error); } get();
